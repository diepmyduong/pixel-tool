export const DEFAULT_STYLE_TEMPLATE = `Pixel art character, top-down 3/4 view (RPG action-game camera angle).
Chibi proportions: large head (~40% of body height), short stocky limbs.
Clean 1-2px dark outline around all silhouettes, no anti-aliasing, flat cel shading (2-3 shades per color, no gradients).
Warm, saturated color palette. Consistent light source from the upper-left across every frame.
Each frame is a single self-contained pose, centered in its cell, same character scale in every cell.`
